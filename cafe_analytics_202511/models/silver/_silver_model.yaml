version: 2

models:
  - name: int_orders
    description: "Order header information extracted from JSON"
    columns:
      - name: order_id
        tests:
          - unique
          - not_null

  - name: int_order_item_options
    description: "Line items with pricing breakdown"


  - name: int_orders_item
    description: "Info for each order,each row represents a single order."