name: 'cafe_analytics_202511'
version: '1.0.0'
profile: 'cafe_analytics_202511'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  cafe_analytics_202511:
    +schema: dbt_medallion
    
    # Bronze layer - raw/landing data
    bronze:
      +materialized: view
      +schema: bronze
      +tags: ["bronze", "landing"]
    
    # Silver layer - cleaned and transformed
    silver:
      +materialized: view
      +schema: silver
      +tags: ["silver", "intermediate"]
    
    # Gold layer - analytics-ready star schema
    gold:
      +materialized: table
      +schema: gold
      +tags: ["gold", "marts"]
      
      dims:
        +tags: ["dimension"]
      
      facts:
        +tags: ["fact"]